[defaults]
inventory = {{ Directories.DirInvent }}/{{ Inventory_filename }}
remote_user = {{ RemoteUser }}

[privilege_escalation]
become = false
become_user = root
become_method = sudo
become_ask_pass = false
